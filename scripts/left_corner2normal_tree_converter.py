from State import Statefrom nltk import treeimport re"""using nltk to build a tree from a left corner parse tree"""ordering = ['j', 'a', 'b', 'f', 'p']last_sample_file = ''def parse_state(state_string, depth):    state = State(depth)    state_string = re.split(':+', state_string)    for item in state_string:        if item == '-' or item == '+':            for variable in ordering:                if variable == 'f' or variable == 'j':                    setattr(state, variable, item)                    break        elif 'ACT' in item:            item  = item.split(';')with open(last_sample_file) as f:    for line in f:        states = line.split('\s')